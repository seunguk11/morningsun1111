import streamlit as st
import numpy as np
import matplotlib.pyplot as plt
import matplotlib.font_manager as fm

# 폰트 설정
font_path = 'font/NanumGothic-Bold.ttf'
fm.fontManager.addfont(font_path)
plt.rcParams['font.family'] = 'NanumGothic'

st.set_page_config(page_title="미분가능 함수와 평균값정리", layout="wide")

st.title("🎓 미분가능 함수와 평균값정리")

st.markdown("""
## 평균값정리 (Mean Value Theorem)

### 📋 정의
닫힌 구간 **[a, b]**에서 **연속**이고 열린 구간 **(a, b)**에서 **미분 가능**한 함수 f(x)에 대해,
다음을 만족하는 점 **c ∈ (a, b)**가 **적어도 하나** 존재합니다:

$$f'(c) = \\frac{f(b) - f(a)}{b - a}$$

즉, 함수의 변화 정도를 나타내는 **기울기(f'(c))**와 **평균 기울기**가 같은 점이 존재한다는 의미입니다.

### 🔍 의미
- **좌변**: 점 c에서의 **순간변화율** (접선의 기울기)
- **우변**: 구간 [a, b]에서의 **평균변화율** (할선의 기울기)

이 정리는 함수의 국소적 성질(미분)과 전역적 성질(구간에서의 변화)을 연결해줍니다.

### ✅ 조건 (중요!)
평균값정리가 성립하기 위한 필수 조건:
1. 함수가 **닫힌 구간 [a, b]에서 연속**
2. 함수가 **열린 구간 (a, b)에서 미분가능**

이 두 조건을 모두 만족하지 않으면 평균값정리가 적용되지 않습니다!

### 📊 예시: 미분가능한 함수

다항식, 삼각함수, 지수함수 등 대부분의 일반적인 함수들이 미분가능합니다.  
다음 페이지에서 **평균값정리 시각화**를 통해 미분가능한 함수에서 평균값정리가 실제로 어떻게 작동하는지 확인해보세요!
""")

st.markdown("""
---
## 📚 추가 학습 자료

### 평균값정리의 중요성
1. **롤의 정리** - 평균값정리의 특수한 경우 (f(a) = f(b)일 때)
2. **함수의 증감** - f'(x) > 0이면 증가, f'(x) < 0이면 감소
3. **극값 판정** - 극대/극소 판정에 사용

### 주의사항 ⚠️
다음 페이지에서 **미분불가능한 함수**에서 평균값정리가 성립하지 않는 경우들을 확인해보세요!
""")

